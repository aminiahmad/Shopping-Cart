# Shopping-Cart

* Shopping cart sample project with:
  > (react, bootstrap, typescript) and use of hooks
* package used:
  > (*vitejs*)
+ install **dependencies** in terminal environment

  - `npm install` 

+ run **Site sample page** in terminal environment

  - `npm run dev` 
